{
  "name": "pgdev",
  "version": "0.0.2",
  "type": "module",
  "description": "PostgreSQL Development Toolchain",
  "license": "MIT",
  "author": "vbilopav",
  "bin": {
    "pgdev": "dist/cli.js"
  },
  "files": [
    "dist/cli.js"
  ],
  "scripts": {
    "dev": "bun run pgdev.ts",
    "typecheck": "bun x tsc --noEmit",
    "build": "bun build ./pgdev.ts --target=bun --minify --outfile dist/cli.js",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "picocolors": "^1.1.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NpgsqlRest/pgdev"
  }
}
